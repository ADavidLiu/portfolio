class Hero {
    constructor() {
        this.hero = $(".hero");
        this.bindEvents();
    }

    bindEvents = () => {
        /* this.hero.paroller({
            factor: '0',
            type: 'foreground',
            direction: 'vertical'
        }); */
    }
}